import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ap.police.mesh',
  appName: 'Eluru Tactical Mesh',
  webDir: 'out',
  server: {
    androidScheme: 'http'
  }
};

export default config;
